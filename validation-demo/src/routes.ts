import { MathomsController } from '@features/mathoms';

// NOTE:
// There is no need to instantiate controllers since importing already triggers
// routing decorators

export const controllers = [
  MathomsController,
];
